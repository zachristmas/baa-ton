#!/bin/sh
# Test stub: the hermetic test runner puts this first on PATH so no test can
# reach a live opencode install. Tests that need opencode behavior inject their own fake.
echo "baa-ton test stub: live opencode is blocked during tests ($*)" >&2
exit 97
