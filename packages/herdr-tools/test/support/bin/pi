#!/bin/sh
# Test stub: the hermetic test runner puts this first on PATH so no test can
# reach a live pi install. Tests that need pi behavior inject their own fake.
echo "baa-ton test stub: live pi is blocked during tests ($*)" >&2
exit 97
